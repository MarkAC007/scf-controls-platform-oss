# FastAPI and Server
# Slice 13: bumped from fastapi==0.109.0 / uvicorn==0.27.0 because the slice
# 13 docling pin requires httpx>=0.28 (via docling-slim[standard]), and
# fastapi 0.109's TestClient calls httpx.Client with the removed `app=`
# kwarg, breaking ~56 backend tests. fastapi>=0.115 switched the TestClient
# to ASGITransport which is compatible with httpx 0.28+.
fastapi>=0.115,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
python-multipart>=0.0.22

# Database
sqlalchemy==2.0.25
asyncpg==0.29.0
psycopg2-binary>=2.9.9  # Sync PostgreSQL driver for Celery tasks
alembic==1.13.1

# Data validation and serialization
# Slice 13: bumped from pydantic==2.5.3 / pydantic-settings==2.1.0 to satisfy
# docling-slim 2.95.0's transitive requirement of pydantic-settings>=2.3.0
# (which itself requires pydantic>=2.7.0). All 661 backend tests pass under
# the new versions; ranges (not exact pins) so dependabot can move them with
# the rest of the ecosystem.
pydantic>=2.13.0,<3.0.0
pydantic-settings>=2.14.0,<3.0.0
email-validator==2.1.0  # Required for EmailStr in Pydantic

# CORS and middleware
python-dotenv==1.2.2

# Rate limiting
slowapi==0.1.9

# Authentication
google-auth==2.27.0
# Slice 13: bumped from httpx==0.26.0 to satisfy docling-slim[standard] 2.95.0
# which requires httpx>=0.28,<1.0.0. All CDM tests (incl. test_cdm_lightrag
# which exercises httpx.Client construction) pass under 0.28.x.
httpx>=0.28,<1.0.0

# Utilities
python-dateutil==2.8.2

# Email
resend==2.4.0

# Redis and Caching
redis>=5.0.0
hiredis>=2.3.0  # C parser for better performance

# Background Tasks (Celery)
celery>=5.3.0
kombu>=5.3.0  # Celery messaging library

# Report export
weasyprint>=62.0
markdown>=3.5

# AWS SDK (Lambda invocation for DPSIA)
boto3>=1.34.0

# Azure Blob Storage (evidence storage on Azure deployments)
azure-storage-blob>=12.19.0

# Testing
# 9.0.3 fixes GHSA-6w46-j5rx-g56g (vulnerable tmpdir handling)
pytest==9.0.3
pytest-asyncio>=0.23.5

# Config / registry files (M2 collector registry, #572)
pyyaml>=6.0

# Malware scanning
pyclamd>=0.4.0

# AI Evidence Assessment
anthropic>=0.39.0
pymupdf>=1.24.0  # PDF text extraction (imported as fitz) — evidence/window/AI paths only (50k cap)
python-docx>=1.1.0  # DOCX text extraction — evidence/window/AI paths only (50k cap)

# Azure Application Insights (prompt monitoring & telemetry)
azure-monitor-opentelemetry>=1.6.0

# SCF catalogue extraction — used by the live "bring your own SCF Excel" import
# task (tasks_catalog.py) so the worker can parse an operator-supplied .xlsx.
# Pinned to match scripts/requirements-importer.txt (the one-shot importer image).
pandas==2.2.3
openpyxl==3.1.5
