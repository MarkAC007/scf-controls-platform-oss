apiVersion: v1
kind: Service
metadata:
  name: {{ include "scf.frontendServiceName" . }}
  labels:
    {{- include "scf.componentLabels" (dict "ctx" . "component" "frontend") | nindent 4 }}
  {{- with .Values.frontend.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "scf.componentSelectorLabels" (dict "ctx" . "component" "frontend") | nindent 4 }}
